// Placeholder. The rustcc driver's interop mode requires at least one
// C++ header in the manifest, even when the crate only uses the
// Rust → C++ direction. See `examples/point_demo/Cargo.toml`.
#pragma once
