/* Minimal freestanding stdlib.h — the kernel only needs size_t/NULL
 * here (allocation goes through heap_4, not malloc). */
#ifndef RUSTCC_STUB_STDLIB_H
#define RUSTCC_STUB_STDLIB_H
#include <stddef.h>
#endif
