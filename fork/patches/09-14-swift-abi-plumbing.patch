diff --git a/compiler/rustc_abi/src/canon_abi.rs b/compiler/rustc_abi/src/canon_abi.rs
index fd45f0ea..1e4ab07e 100644
--- a/compiler/rustc_abi/src/canon_abi.rs
+++ b/compiler/rustc_abi/src/canon_abi.rs
@@ -25,6 +25,18 @@ pub enum CanonAbi {
     // allowing a `match` to easily exhaustively ignore these subcategories of variants.
     // Otherwise it is very tempting to avoid matching exhaustively!
     C,
+    /// The Itanium C++ ABI. Today every codegen-side code path
+    /// treats it identically to `C` — the two calling conventions
+    /// agree for scalar/pointer arguments on SysV AMD64. The
+    /// variant exists as a hook point for P09+ work that will
+    /// add sret/this-pointer/record-by-value handling specific
+    /// to Itanium, without retroactively corrupting the plain
+    /// `extern "C"` path.
+    Cpp,
+    /// The Swift ABI (Phase 1). Treated identically to `C` for
+    /// codegen today; distinct variant lets the symbol-mangling
+    /// path route to `swift::mangle` for `$s`-prefixed names.
+    Swift,
     Rust,
     RustCold,
     RustPreserveNone,
@@ -57,6 +69,8 @@ pub fn is_rustic_abi(self) -> bool {
         match self {
             CanonAbi::Rust | CanonAbi::RustCold | CanonAbi::RustPreserveNone => true,
             CanonAbi::C
+            | CanonAbi::Cpp
+            | CanonAbi::Swift
             | CanonAbi::Custom
             | CanonAbi::Arm(_)
             | CanonAbi::GpuKernel
@@ -73,6 +87,8 @@ fn fmt(&self, f: &mut fmt::Formatter<'_>) -> fmt::Result {
         // that we need to generate error messages.
         let erased_abi = match self {
             CanonAbi::C => ExternAbi::C { unwind: false },
+            CanonAbi::Cpp => ExternAbi::Cpp { unwind: false },
+            CanonAbi::Swift => ExternAbi::Swift { unwind: false },
             CanonAbi::Rust => ExternAbi::Rust,
             CanonAbi::RustCold => ExternAbi::RustCold,
             CanonAbi::RustPreserveNone => ExternAbi::RustPreserveNone,
diff --git a/compiler/rustc_abi/src/extern_abi.rs b/compiler/rustc_abi/src/extern_abi.rs
index 95ba2ee3..642048a2 100644
--- a/compiler/rustc_abi/src/extern_abi.rs
+++ b/compiler/rustc_abi/src/extern_abi.rs
@@ -23,6 +23,24 @@ pub enum ExternAbi {
     C {
         unwind: bool,
     },
+    /// The Itanium C++ ABI. Items in `extern "C++"` blocks get
+    /// Itanium-mangled symbol names via `rustc_abi_cxx::mangle`,
+    /// and (once P09 lands) sret/this-pointer/record-by-value
+    /// call-convention lowering. Valid on targets whose C++
+    /// runtime follows Itanium (Clang on Linux/Darwin/iOS).
+    Cpp {
+        unwind: bool,
+    },
+    /// The Swift ABI (Phase 1 — trivial-type subset). Items in
+    /// `extern "Swift"` blocks get Swift-mangled symbol names
+    /// (`$s` prefix) via the Swift mangler, and use the C
+    /// calling convention as an approximation (Swift's real CC
+    /// passes `self` in r13 and has context/error registers, but
+    /// for trivially-copyable scalars it degenerates to C). Full
+    /// VWT-aware support is Phase 2.
+    Swift {
+        unwind: bool,
+    },
     /// ABI of the "system" interface, e.g. the Win32 API, always "aliasing"
     System {
         unwind: bool,
@@ -172,6 +190,10 @@ fn from_str(s: &str) -> Result<$e_name, Self::Err> {
     ExternAbi = {
             C { unwind: false } =><= "C",
             C { unwind: true } =><= "C-unwind",
+            Cpp { unwind: false } =><= "C++",
+            Cpp { unwind: true } =><= "C++-unwind",
+            Swift { unwind: false } =><= "Swift",
+            Swift { unwind: true } =><= "Swift-unwind",
             Rust =><= "Rust",
             Aapcs { unwind: false } =><= "aapcs",
             Aapcs { unwind: true } =><= "aapcs-unwind",
@@ -332,6 +354,8 @@ pub fn supports_guaranteed_tail_call(self) -> bool {
                 false
             }
             Self::C { .. }
+            | Self::Cpp { .. }
+            | Self::Swift { .. }
             | Self::System { .. }
             | Self::Rust
             | Self::RustCall
diff --git a/compiler/rustc_ast_lowering/src/stability.rs b/compiler/rustc_ast_lowering/src/stability.rs
index d185cc91..05513a1b 100644
--- a/compiler/rustc_ast_lowering/src/stability.rs
+++ b/compiler/rustc_ast_lowering/src/stability.rs
@@ -69,6 +69,8 @@ pub fn extern_abi_stability(abi: ExternAbi) -> Result<(), UnstableAbi> {
         // stable ABIs
         ExternAbi::Rust
         | ExternAbi::C { .. }
+        | ExternAbi::Cpp { .. }
+        | ExternAbi::Swift { .. }
         | ExternAbi::Cdecl { .. }
         | ExternAbi::Stdcall { .. }
         | ExternAbi::Fastcall { .. }
diff --git a/compiler/rustc_ast_passes/src/ast_validation.rs b/compiler/rustc_ast_passes/src/ast_validation.rs
index 0541d393..ca3307e3 100644
--- a/compiler/rustc_ast_passes/src/ast_validation.rs
+++ b/compiler/rustc_ast_passes/src/ast_validation.rs
@@ -421,6 +421,8 @@ fn check_extern_fn_signature(&self, abi: ExternAbi, ctxt: FnCtxt, ident: &Ident,
             AbiMapping::Direct(canon_abi) | AbiMapping::Deprecated(canon_abi) => {
                 match canon_abi {
                     CanonAbi::C
+                    | CanonAbi::Cpp
+                    | CanonAbi::Swift
                     | CanonAbi::Rust
                     | CanonAbi::RustCold
                     | CanonAbi::RustPreserveNone
diff --git a/compiler/rustc_codegen_llvm/src/abi.rs b/compiler/rustc_codegen_llvm/src/abi.rs
index dcde9602..216e0b1e 100644
--- a/compiler/rustc_codegen_llvm/src/abi.rs
+++ b/compiler/rustc_codegen_llvm/src/abi.rs
@@ -712,7 +712,12 @@ fn get_param(&mut self, index: usize) -> Self::Value {
 /// ABI, for the current target.
 pub(crate) fn to_llvm_calling_convention(sess: &Session, abi: CanonAbi) -> llvm::CallConv {
     match abi {
-        CanonAbi::C | CanonAbi::Rust => llvm::CCallConv,
+        // P09 scaffold: CanonAbi::Cpp shares the C calling
+        // convention at the LLVM level. Per-target Itanium
+        // divergence (sret for non-POD returns, etc.) is handled
+        // at the ABI-info layer, not here — LLVM's `CCallConv` is
+        // fine as long as the caller/callee agree on the shape.
+        CanonAbi::C | CanonAbi::Cpp | CanonAbi::Swift | CanonAbi::Rust => llvm::CCallConv,
         CanonAbi::RustCold => llvm::PreserveMost,
         CanonAbi::RustPreserveNone => match &sess.target.arch {
             Arch::X86_64 | Arch::AArch64 => llvm::PreserveNone,
diff --git a/compiler/rustc_hir_typeck/src/callee.rs b/compiler/rustc_hir_typeck/src/callee.rs
index e5fcc68d..90e509ed 100644
--- a/compiler/rustc_hir_typeck/src/callee.rs
+++ b/compiler/rustc_hir_typeck/src/callee.rs
@@ -195,6 +195,8 @@ pub(crate) fn check_call_abi(&self, abi: ExternAbi, span: Span) {
             }
 
             CanonAbi::C
+            | CanonAbi::Cpp
+            | CanonAbi::Swift
             | CanonAbi::Rust
             | CanonAbi::RustCold
             | CanonAbi::RustPreserveNone
diff --git a/compiler/rustc_middle/src/ty/layout.rs b/compiler/rustc_middle/src/ty/layout.rs
index 53706cc8..be3f4f7e 100644
--- a/compiler/rustc_middle/src/ty/layout.rs
+++ b/compiler/rustc_middle/src/ty/layout.rs
@@ -1173,6 +1173,36 @@ fn is_scalable_vector(this: TyAndLayout<'tcx>) -> bool {
     fn is_pass_indirectly_in_non_rustic_abis_flag_set(this: TyAndLayout<'tcx>) -> bool {
         matches!(this.ty.kind(), ty::Adt(def, _) if def.repr().flags.contains(ReprFlags::PASS_INDIRECTLY_IN_NON_RUSTIC_ABIS))
     }
+
+    /// True when this type is "non-trivial for C++ calls" per
+    /// the Itanium ABI. Itanium passes such types by invisible
+    /// reference regardless of size. P09 uses this to force
+    /// `PassMode::Indirect` in `ExternAbi::Cpp` fn signatures.
+    ///
+    /// Triggers (both for `#[repr(cpp)]` ADTs):
+    /// - a user-declared `Drop` impl, or
+    /// - `!Copy`. A C++ class with any hand-rolled copy/move ctor
+    ///   is non-trivial, and Rust's closest proxy is "doesn't
+    ///   implement `Copy`": a trivially-copyable Rust type is
+    ///   `Copy`-safe, and a `#[repr(cpp)]` backed by a non-trivial
+    ///   C++ class will not be `Copy` (copying it would be a
+    ///   memcpy, but the C++ side's copy ctor wants to run).
+    fn is_cxx_non_trivial_for_calls(this: TyAndLayout<'tcx>, cx: &C) -> bool {
+        match this.ty.kind() {
+            ty::Adt(def, _) => {
+                if !def.repr().cpp() {
+                    return false;
+                }
+                if def.destructor(cx.tcx()).is_some() {
+                    return true;
+                }
+                // `!Copy` → non-trivial. `type_is_copy_modulo_regions`
+                // checks the `Copy` bound in the current typing env.
+                !cx.tcx().type_is_copy_modulo_regions(cx.typing_env(), this.ty)
+            }
+            _ => false,
+        }
+    }
 }
 
 /// Calculates whether a function's ABI can unwind or not.
@@ -1252,6 +1282,8 @@ pub fn fn_can_unwind(tcx: TyCtxt<'_>, fn_def_id: Option<DefId>, abi: ExternAbi)
     use ExternAbi::*;
     match abi {
         C { unwind }
+        | Cpp { unwind }
+        | Swift { unwind }
         | System { unwind }
         | Cdecl { unwind }
         | Stdcall { unwind }
diff --git a/compiler/rustc_public/src/unstable/convert/stable/abi.rs b/compiler/rustc_public/src/unstable/convert/stable/abi.rs
index d8c4cee7..747106de 100644
--- a/compiler/rustc_public/src/unstable/convert/stable/abi.rs
+++ b/compiler/rustc_public/src/unstable/convert/stable/abi.rs
@@ -122,6 +122,12 @@ impl<'tcx> Stable<'tcx> for CanonAbi {
     fn stable(&self, _: &mut Tables<'_, BridgeTys>, _: &CompilerCtxt<'_, BridgeTys>) -> Self::T {
         match self {
             CanonAbi::C => CallConvention::C,
+            // P09 scaffold: stable-API clients see CanonAbi::Cpp
+            // as C until the public API grows its own variant.
+            // Mirrors how ExternAbi::Cpp is lowered in the
+            // matching ExternAbi conversion.
+            CanonAbi::Cpp => CallConvention::C,
+            CanonAbi::Swift => CallConvention::C,
             CanonAbi::Rust => CallConvention::Rust,
             CanonAbi::RustCold => CallConvention::Cold,
             CanonAbi::RustPreserveNone => CallConvention::PreserveNone,
diff --git a/compiler/rustc_public/src/unstable/convert/stable/ty.rs b/compiler/rustc_public/src/unstable/convert/stable/ty.rs
index 9a9576d4..303c9408 100644
--- a/compiler/rustc_public/src/unstable/convert/stable/ty.rs
+++ b/compiler/rustc_public/src/unstable/convert/stable/ty.rs
@@ -1020,6 +1020,13 @@ fn stable(&self, _: &mut Tables<'_, BridgeTys>, _: &CompilerCtxt<'_, BridgeTys>)
         match *self {
             ExternAbi::Rust => Abi::Rust,
             ExternAbi::C { unwind } => Abi::C { unwind },
+            // P08: until `rustc_public` grows its own `Abi::Cpp`
+            // variant, stable-API clients see `extern "C++"` as the
+            // C ABI. Correct for the CanonAbi lowering this fork
+            // does today (extern "C++" uses CanonAbi::C); refine
+            // when the public API picks up a C++-specific variant.
+            ExternAbi::Cpp { unwind } => Abi::C { unwind },
+            ExternAbi::Swift { unwind } => Abi::C { unwind },
             ExternAbi::Cdecl { unwind } => Abi::Cdecl { unwind },
             ExternAbi::Stdcall { unwind } => Abi::Stdcall { unwind },
             ExternAbi::Fastcall { unwind } => Abi::Fastcall { unwind },
diff --git a/compiler/rustc_target/src/spec/abi_map.rs b/compiler/rustc_target/src/spec/abi_map.rs
index c0fdd946..4d913cce 100644
--- a/compiler/rustc_target/src/spec/abi_map.rs
+++ b/compiler/rustc_target/src/spec/abi_map.rs
@@ -83,6 +83,16 @@ pub fn canonize_abi(&self, extern_abi: ExternAbi, has_c_varargs: bool) -> AbiMap
         let canon_abi = match (extern_abi, arch) {
             // infallible lowerings
             (ExternAbi::C { .. }, _) => CanonAbi::C,
+            // P09 scaffold: Itanium C++ ABI gets its own CanonAbi
+            // variant. Today every per-target call-conv lowering
+            // treats `CanonAbi::Cpp` identically to `CanonAbi::C`
+            // (correct for scalar/pointer shapes on SysV AMD64).
+            // The distinct variant exists so future per-target
+            // patches can add sret / this-pointer / record-by-
+            // value handling for records without perturbing the
+            // plain `extern "C"` path.
+            (ExternAbi::Cpp { .. }, _) => CanonAbi::Cpp,
+            (ExternAbi::Swift { .. }, _) => CanonAbi::Swift,
             (ExternAbi::Rust | ExternAbi::RustCall, _) => CanonAbi::Rust,
             (ExternAbi::Unadjusted, _) => CanonAbi::C,
 
